import React from "react";
import ReactDOM from "react-dom/client";
import AxiosAwait from "./AxiosAwait";


const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <div className="container">
    <AxiosAwait />
  </div>
);